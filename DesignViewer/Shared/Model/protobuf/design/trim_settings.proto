
syntax = "proto3";

package design;

import "math/sides.proto";

option csharp_namespace = "Model.Design";

// A TrimSettings is a part of Artboard that defines its shape and trim.
message TrimSettings
{
  math.Sides bleed = 1;
  math.Sides slug = 2;
}
